export const config = {
  aws: {
    region: "us-east-1",
    accessKeyId: "",
    secretAccessKey: "",
    // sessionToken: "" // Add this if using temporary credentials
  },
};

export default config;
